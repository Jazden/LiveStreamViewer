Simple live stream viewer, comes with a set of default streams
Users can add their own streams (youtube links, livestream addresses) under settings. Stream data is stored locally
Can open site on mobile phone & link with QR code, this will allow remote control of streams and layout from phone.
Can also link a primary & secondary display together so they'll show same streams & layout
